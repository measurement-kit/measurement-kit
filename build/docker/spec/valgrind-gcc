#!/bin/sh
set -e
REPOROOT=$(cd $(dirname $(dirname $(dirname $(dirname $0)))) && pwd -P)
. $REPOROOT/build/docker/template/gcc

debian_deps="$debian_deps valgrind"

export configure_flags="--disable-shared"

subr_make_check() {
    make $make_flags run-valgrind V=0
}
