machine:
  services:
    - docker

#general:
#  branches:
#    ignore:
#      - /^doc\/.*$/

test:
  override:
    - ./build/docker/valgrind-clang
    - if test -f ./test-suite.log; then cat ./test-suite.log; fi
